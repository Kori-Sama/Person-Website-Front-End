# Personal Website created by KoriSama
It's only a website UI, can not handle data temporally

## Frontend->React + Mobx + Material UI + TypeScript
## Backend->C# + ASP.net core + EntityFramework + MySql